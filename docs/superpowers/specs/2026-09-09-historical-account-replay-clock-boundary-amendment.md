# Clock-boundary amendment — separately recorded attempt

The first actual account replay failed at 09:54:13 UTC on 9 September 2026. Its last accepted BTC close was 8 February 2018 00:00 UTC. Its source remains frozen in `.worktrees/historical-account-replay` at commit `46818a2`, source identity `a9b9d378de119b0a89a02cc28dbbf393189f9ab9ecc490bf6a3d88d6cf0ce990`. Its registered attempt, protocol, original discovery, partial event logs, failures and external log remain intact in `HistoricalReplays/round-001-20260909` and `HistoricalReplayLogs/round-001-20260909.log`.

## Root cause and decision

The existing archive loader accepts intervals lasting exactly one hour. The new replay engine additionally requires UTC clock alignment. A read-only timestamp audit of all 260 checksum-pinned files found 42 exact-duration but off-clock rows per asset around a February 2018 data gap. No loader-valid overlap or duplicate exists. The 15 invalid-duration rows per asset were already excluded by the original parser. This identifies the data discrepancy, not an independently verified cause of the provider's interruption.

BTC February archive SHA-256: `4e040b57a38af99b1f86efb12d95aed406b575af67b8038831523ff6362225d1`; CSV lines 170–211, open milliseconds `1518168494789 + 3600000*k`, `k=0..41`. ETH archive SHA-256: `e1fe7f0dd649f4bfc94c43e071d8c47360bdc9f6e63b8b5926c316490bb99220`; corresponding opens `1518168494800 + 3600000*k`. Their first opens are 9 February 2018 09:28:14.789/.800 UTC; last opens are 11 February 2018 02:28:14.789/.800 UTC.

Choose a uniform data-quality rule, not a P&L-driven selection: keep the strict UTC-clock engine; quarantine off-clock rows without retimestamping, resampling, filling or substituting prices. Preserve their original parsed contents and archive identities. Treat their absence as a gap under the existing cancellation/liquidation policy. Do not relax the clock guard to finish the run. This changes the executable input selection, so it is an explicitly amended, separately registered hypothesis—not a continuation or replacement of the failed attempt.

## Unchanged boundaries

The original live paper study, its source/environment/process/ledger/registry/rules and fixed end remain untouched. The first historical replay's source and output also remain untouched. No installs, account credentials, orders, alerts or power changes. The two retained failed-screen candidates, costs, sizing, stop/target/expiry rules, strict one-hour engine, causal prefix calculations, source/runtime binding, original discovery SHA, 260 archive pins and raw parser counts remain unchanged. No strategy search or optimization.

## Implementation contract

Only the historical runner, reporting helper and runner tests change. Do not modify the shared Binance archive parser, core replay engine, strategy definitions or existing historical discovery.

1. Keep exact archive preflight and returned manifest/row-count validation before transformation. Each asset still has 79,270 parser-valid raw rows; do not rewrite its original manifest's selected-row counts.
2. A separate preparation step partitions those rows by UTC clock alignment. Persist all quarantined rows with original timestamps, archive filename/digest and deterministic row fingerprints in an exclusive `data-quality.json` before evaluating any account. Record raw selected counts, executable counts, excluded invalid-duration counts and quarantined counts. The source/protocol bind the fixed quarantine policy; the preflight status and final result bind the actual quality artifact hash. The original pinned archives remain unmodified.
3. Validate all retained rows against the engine input contract before launching asset calculations, including exact duration/availability, envelope, positive finite coherent OHLC, nonnegative volume, finalized initial revision and ordered nonoverlap. Unexpected defects fail preflight and preserve the attempt, not silently drop additional data. Quarantine is limited to off-clock starts; duration defects remain the existing parser's separately reported exclusions.
4. Use hypothesis `fixed_selected_rule_clock_aligned_account_replay_v2`. Every new protocol explicitly records the amendment, reason and failed original source identity/hypothesis. When the actual same-parent registry contains the original attempt, retain a parent reference with its attempt number/protocol identity. Keep the full prior 24-trial history and old failure. Fresh reproductions must still disclose that this v2 rule arose after the v1 failure; never reset or conceal selection history.
5. Run only the retained clock-aligned rows through the unchanged engine. The complete pinned dataset should yield 79,228 executable bars per asset; 188 requested-window missing hours (4 leading plus 184 internal) and 31 internal gaps. A held position spanning the quarantined interval must liquidate at the next observed opening under the existing gap policy, retaining its loss/taint; pending entries cancel. Preserve the gap, do not pretend that it was a series of tradable synthetic hours.
6. Reports and machine results distinguish 79,270 raw parser-valid rows, 42 quarantined off-clock rows, 79,228 executable rows and 15 already excluded invalid-duration rows per asset. Keep all existing account/period/coverage/execution caveats and label this amended retrospective evaluation. No confidence, independent validation or promotion.
7. Add synthetic regressions for off-clock exact-duration rows, deterministic retained quarantine records, unchanged raw manifest pins/counts, preflight rejection of other invalid rows, failed-v1-to-v2 parent linkage, and strict engine rejection still intact. Test gap cancellation/liquidation by the existing engine on prepared rows without a full historical evaluation. Also synchronize a sibling worker in the failure test and explicitly assert termination and retained partial evidence, closing the prior minor coverage gap in this bounded correction.

## Execution and evidence

This amendment is implemented in `.worktrees/historical-account-replay-clock-boundaries`, a new isolated checkout. Review the correction diff once, scoped to this boundary issue and directly introduced effects; no repeat broad review or historical search. Before results, freeze the corrected source and register `HistoricalReplays/round-002-20260909` using the exact existing archive cache in offline mode and two independent asset workers. Preserve every outcome and failed attempt. Independently reconcile all final account flows, event hashes, periods and raw-to-executable coverage. Full final-source release tests, deterministic fixtures and app packaging follow; previous source's interrupted suite is not a passing final suite.
